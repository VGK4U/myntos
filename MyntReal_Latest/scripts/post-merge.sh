#!/bin/bash
set -e

echo "[POST-MERGE] Post-merge setup complete. Backend handles DB migrations on startup."
echo "[POST-MERGE] Python dependencies pre-installed in Nix environment."
echo "[POST-MERGE] Done."
