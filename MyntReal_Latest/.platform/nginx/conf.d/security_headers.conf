server_tokens off;
add_header X-Content-Type-Options "nosniff" always;

# SSL configurations (Will apply if Nginx terminates HTTPS)
ssl_protocols TLSv1.2 TLSv1.3;
ssl_ciphers HIGH:!aNULL:!MD5:!CBC:!SHA1;
ssl_prefer_server_ciphers on;
